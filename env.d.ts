/// <reference types="vite/client" />

// Gives `import.meta.env` typing (used by the commented real-axios block in
// src/shared/api.ts) and declares the ambient modules Vite injects. vue-tsc
// understands `*.vue` files natively, so no `*.vue` shim is needed here.
